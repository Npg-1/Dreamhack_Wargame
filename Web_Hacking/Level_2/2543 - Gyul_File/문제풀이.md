```
문제 설명
귤 농장을 운영하던 관리자는, 최근 자신의 웹사이트에 수상한 접속이 많다는 사실을 알게 되었습니다.
그래서 직접 "관리자 로그인 페이지"와 "접속 로그 뷰어"를 만들어 관리하려고 합니다.

관리자는 보안을 강화하고자, WAF 방화벽을 설정할 때 필터링을 이렇게 설정 했습니다.
"--",
"/", "/",
" or ",
" and ",
"union",
"select",
"insert",
"update",
"delete",
"drop"

관리자가 설정한 보안을 뚫고 flag.txt를 획득하세요!
```

일단 문제 설명먼저 보면 뭐 안 되는 게 많음. 두 번째에 /는 왜 두번이나 했는지는 모르겠지만 2번이나 할 정도로 하지 말라는 뜻인가봄ㄷㄷ

필터들을 보면 --와 /이 막혀있어서 주석은 못 쓰는 거 같고, or도 필터링이 되어있음. 근데 그냥 or이 아니라 ' or '이 필터링 되어 있는 거라서 'or'만 쓴다면 쓸 수 있음

그러면 로그인창에서 국룰 username을 입력해보면 `' or 1=1--` 당연히 안 됨, or과 -- 때문에 안 됨 여기서 or의 양쪽 공백을 없애면
`SELECT * FROM mikang WHERE username=''or1=1--' and password='pa';`가 되는데 이러면 or이 인식이 안 됨. 근데 괄호를 쓰게 된다면?? 이게 된다
`SELECT * FROM mikang WHERE username=''or(1=1)--' and password='pa';`이러면 되는 거임ㄷㄷ

이제 주석만 제거하면 되는데 주석을 없애면 뒤의 and와 이어지지 않는 데스우 이제 뒷 부분도 or로 처리하면 로그인이 됨

로그인을 하면 이런 화면이 나오는데 여기서 따로 볼 건 없고 url에서 현재 위치가 logs에 있는지 확인해보고 없다면 상위, 또 상위로 올라가면 따단!

B1N4RY{This_is_4_s2cr2t_GyuL_Flag_By_LFi_tRaV2Rs4L_!!}
